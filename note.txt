Legacy note file.

Use these newer files instead:
- README.md
- docs\quick-reference.md
- config\scrcpy.conf
- launch-scrcpy.bat

Common commands:
adb devices
adb reboot download
adb reboot recovery
adb reboot --poweroff
scrcpy --max-size 1024
scrcpy --video-bit-rate 32M --window-width 1080
